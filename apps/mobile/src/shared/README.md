# Mobile Shared

This directory is for reusable mobile primitives and infrastructure.

Status: shell only.

Feature-specific behavior must stay in `features/`.

Existing runtime code remains in its current location until a focused extraction task moves it safely.

Do not copy Vite/browser-only prototype behavior into this folder.
