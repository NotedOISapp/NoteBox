# Navigation

Status: shell only.

Purpose:
Shared navigation utilities, typed route helpers, and deep-link resolution logic used across features.

Allowed:
- reusable, cross-feature code only

Forbidden:
- feature-specific business logic
- screen-specific state
- API behavior owned by one feature
- product doctrine changes
- prototype-only runtime behavior
- Vite/browser-only behavior
